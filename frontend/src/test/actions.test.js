import React from 'react'
import { fireEvent } from '@testing-library/react'

test ('Register', () => {
    var Reg = {"Username": "test_username", "Password": "test_password"}
    var json_Reg = json.Stringfy(Reg)

})